<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial; background: #f4f4f4; padding: 20px; }
        .box {
            max-width: 480px; margin: auto; background: #fff; padding: 25px;
            border-radius: 12px; box-shadow: 0 4px 10px rgba(0,0,0,0.1);
        }
        .title { font-size: 22px; font-weight: bold; color: #333; }
        .otp {
            font-size: 32px; font-weight: bold; letter-spacing: 5px;
            background: #f0f0f0; padding: 12px; border-radius: 8px; text-align: center;
        }
        .footer { margin-top: 30px; color: #888; font-size: 13px; text-align: center; }
    </style>
</head>
<body>
    <div class="box">
        <div class="title">Reset Your Password</div>
        <p>Hello,</p>
        <p>Use the following OTP to reset your password:</p>

        <div class="otp"><%= otp %></div>

        <p>This code will expire in <b>5 minutes</b>.</p>

        <div class="footer">
            © <%= new Date().getFullYear() %> Explorer | Secure Account System
        </div>
    </div>
</body>
</html>

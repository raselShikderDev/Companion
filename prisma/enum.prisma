enum Role {
    SUPER_ADMIN 
    ADMIN 
    USER
}

enum TripStatus {
    PLANNED
    COMPLETED
    CANCELLED
}

enum MatchStatus {
    PENDING
    ACCEPTED
    REJECTED
    BLOCKED
}

enum UserStatus {
    ACTIVE
    BLOCKED
    SUSPENDED
}

enum PaymentStatus {
    PAID
    UNPAID
}

enum Gender {
    MALE
    FEMALE
}

enum LookingFor {
    MALE
    FEMALE
    ANYONE
}
